/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import FormulárioCódigo from './visual/FormulárioCódigo'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => FormulárioCódigo);
